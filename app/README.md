# API

Contiene el código fuente de la FastAPI que expone el modelo entrenado para realizar inferencias. 
Aquí también suele ubicarse la documentación de los endpoints y los archivos del modelo serializado que la API carga al iniciar.

